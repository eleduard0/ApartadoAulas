@model List<CarreraModel>
@{
    ViewData["Title"] = "Listar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card col-8">
    <div class="card-header">
        Lista de Carreras disponibles
    </div>
    <div class="card-body">
        <a asp-action="Guardar" asp-controller="Carrera" class="btn btn-sn" id="button">Agregar edificio</a>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem=>item.Nombre)</td>
                        <td>
                            <a asp-action="Editar" asp-controller="Carrera" asp-route-IdCarrera="@item.IdCarrera" class="btn btn-warning btn-sn">Editar</a>
                            <a asp-action="Eliminar" asp-controller="Carrera" asp-route-IdCarrera="@item.IdCarrera" class="btn btn-danger btn-sn">Eliminar</a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

